data = {
  profile = "slick.jdbc.MySQLProfile$"
  db = {
    url = "jdbc:mysql://localhost/durooma?useSSL=false"
    driver = com.mysql.jdbc.Driver
    user = "durooma"
    password = ""
  }
  codegen = {
    package = "com.durooma.db"
  }
}